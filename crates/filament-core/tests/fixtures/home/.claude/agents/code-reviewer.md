---
name: code-reviewer
description: A user-level code reviewer that is overridden by the project-level one.
model: haiku
color: blue
---

This user-scoped agent shares the name `code-reviewer` with a project agent, so
it should be marked as shadowed by the project definition.
